# Copy to .env and fill in. .env is gitignored and never leaves this machine.
# Environment variables win over this file, so containers and CI are unaffected.
#
# Prefer an account scoped to what the scripts actually need over an admin or
# superuser account. See docs/CREDENTIALS.md.
